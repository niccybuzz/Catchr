document.addEventListener("DOMContentLoaded", () => {
    const animatedGrid = document.getElementById("gridDiv");
    
    // Show the div

        animatedGrid.classList.add("opacity-100"); 
  

  });